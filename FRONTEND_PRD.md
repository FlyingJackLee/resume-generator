# 简历生成器 · 统一前端功能 PRD（Phase 1）

> 本文档面向 UI/UX 设计师，只描述"要做什么、为什么、每个页面要展示/操作什么内容"，
> **不涉及视觉风格**（配色、字体、组件库、间距等）——视觉设计另行讨论。
>
> 配套的工程侧分期路线图见 `FRONTEND_ROADMAP.md`（不需要设计师阅读）。

## 0. 一句话背景

这是一个"事实安全"的简历定制工具：`web/data/resume.yaml`（Master Resume）是唯一真实数据源，
永远只读。针对每个目标岗位（JD），Agent 会跑一套多阶段流程，从 Master Resume 里挑选、改写、
重排内容生成一份"目标简历"，但**绝不能编造 Master Resume 里没有的事实**。整个流程有两处需要人工
确认（"Human Gate"），因为最终要不要用某个改写建议、要不要人工调整，是人的判断，不是机器自动
决定的。

现在要做的，是把这套流程从现在极简的服务端网页（`agent/templates/*.html`，裸表单+JSON文本框）
升级成一个正式好用的前端。

## 1. 名词表

| 术语 | 含义 |
|---|---|
| Master Resume | 唯一真实简历数据，只读，存于 `web/data/resume.yaml` |
| JD | Job Description，目标岗位描述，用户粘贴进来的原始文本 |
| Run | 针对一个 JD 发起的一次完整处理流程，有唯一 ID，产出物存在自己的文件夹里 |
| Facts | Master Resume 里每条经历/成果被拆解出的"事实单元"，每次改写都要能追溯到具体 Fact |
| Human Gate ① | 策略审批关卡：AI 生成"改写策略"后，人工确认要不要按这个策略执行 |
| Human Gate ② | 最终审批关卡：AI 按策略生成候选简历、通过事实校验和评分后，人工做最终决定（批准导出/拒绝/手动微调） |
| Rewrite Strategy | Human Gate ① 审批的对象：一组"改写动作"（提升/改写/重排/降权/保留），带优先级和理由 |
| Patch | 对简历的具体修改操作（replace/reorder/hide/restore），是唯一允许改动候选简历的方式，永远经过事实校验 |
| Fact Validator | 自动校验器，检查候选简历有没有编造事实、篡改数字、破坏受保护字段 |
| Hiring Manager | 打分节点，模拟招聘经理视角给候选简历打分（满分 100，7 个维度） |

## 2. 目标与范围

**目标**：把现有能跑通但界面简陋的两个系统（`web/` 静态生成 + `agent/` FastAPI 服务）整合成一个
本人日常会用、值得信赖的正式工具，覆盖"发起分析 → 审批策略 → 审批最终版 → 在线查看/对比不同版本"
全流程。

**使用场景**：纯个人工具，本地运行，不对外分享、不考虑陌生人访问、不考虑多用户。

**本文档范围（Phase 1，"核心可用"）**：
1. Agent 操作台：Run 列表、新建 Run、全流程实时进度、Human Gate ①、Human Gate ②
2. 在线查看：任选一个版本（含 Master Resume 本身）在浏览器里查看排版效果，支持中英文切换
3. LangSmith 调用链路外链入口

**明确不在本文档范围内（见第 6 节）**：视觉风格设计、鉴权/远程访问、多用户、Run 管理增强
（搜索/分组/标记/归档/跨版本对比——这些是 Phase 2，见 `FRONTEND_ROADMAP.md`）。

## 3. 整体信息架构

顶层两个区域，常驻导航可以互相切换：

- **Agent 操作台**：Run 列表是默认首页；点进某个 Run 是详情页，页面内容随该 Run 所处阶段动态变化
- **在线查看**：独立入口，选择"Master Resume"或任意一个 Run 的产出版本，查看排版后的简历

两个区域共享同一个后端服务、同一套导航壳，但"在线查看"页面内嵌的简历渲染内容，视觉上和
`web/` 现有 PDF 导出应完全一致（同一套排版逻辑复用，非重新设计）。

## 4. 页面详情

### 4.1 Run 列表（操作台首页）

**内容**：所有 Run 的列表，每行/每张卡片展示：
- JD 标识（用户自己起的名字，比如"Google AI Agent"）
- 公司（可能为空 / "未分类"，见 4.2 说明）
- 当前状态（见第 7 节状态机，需要设计成一目了然的徽章：进行中 / 待审批 / 已完成 / 失败 / 已中断 / 已拒绝）
- Hiring Manager 总分（还没跑到这一步则显示"-"）
- 创建时间

**操作**：点击某一行进入该 Run 的详情页；页面顶部/角落有"新建 Run"入口。

**空状态**：一个 Run 都没有时，给出引导文案和"新建 Run"的醒目入口。

### 4.2 新建 Run

**表单字段**：
- JD 标识（必填，短文本，用户自己起的便于识别的名字）
- 公司（可选，短文本；不填的话，等分析流程跑完后，系统会用 AI 从 JD 里识别出的公司名自动
  回填；如果自动识别也没有，就归入"未分类"）
- 岗位 JD 原文（必填，长文本粘贴框）

**提交后**：跳转到该 Run 的详情页，分析流程立刻在后台开始跑（无需用户等待表单页面）。

### 4.3 Run 详情页 — 分析阶段

覆盖流程的前半段：JD Analyzer → Resume Matcher → HR Reviewer → Rewrite Strategy 生成，
直到进入 Human Gate ①。

**顶部固定区**：JD 标识、公司、状态徽章、当前处于哪个阶段（文字描述，比如"正在运行：Resume
Matcher"）、进度条（4 个阶段，实时更新，见第 8 节"实时更新"）。

**过程产出**（每个阶段跑完就应该能展开查看，不用等全部跑完）：
- **岗位画像**（JD Analyzer 产出）：目标公司、目标角色、资历级别、需求列表（每条需求标注
  must_have / preferred / responsibility，附权重）
- **匹配报告**（Resume Matcher 产出）：针对每条岗位需求，标注匹配状态（完全匹配/部分匹配/
  缺失）+ 置信度 + 对应的 Master Resume 事实 + 简要理由
- **HR 评审**（HR Reviewer 产出）：优势点、薄弱点、缺失的关键词、建议优先修改的方向

### 4.4 Human Gate ① — 策略审批页

进入条件：状态为"等待策略确认"。

**展示内容**：
- 岗位定位陈述（一段文字，可编辑）
- 允许使用的关键词 / 禁止使用的关键词（两组标签式列表，可编辑增删）
- 拟执行的修改动作列表，每条展示：
  - 动作类型（提升 promote / 改写 rewrite / 重排 reorder / 降权 deprioritize / 保留 preserve）
  - 目标字段（用可读的名称展示，比如"工作经历 › 某某公司 › 职责第 2 条"，而不是内部路径字符串）
  - 优先级（1 最高 - 5 最低，可编辑）
  - 修改说明（AI 给出的改写指导语，可编辑）
  - 支撑事实（这条改写依据 Master Resume 里的哪些具体事实，只读展示，用于人工核实"这条建议
    有没有依据"）
  - 一个"保留/丢弃"的开关——**不支持新增一条 AI 没建议的动作**，只能在已有建议里做取舍和微调

**操作**：
- "保存修改"：只更新策略内容，不推进流程
- "批准并生成简历"：确认这一版策略，流程推进到编译阶段（Resume Editor 开始工作）

> **设计稿评审确认（2026-09-02）**：不做"策略对齐度评分"（Alignment Score）和"自动审批检查清单"
> （Approval Checklist）这类对 AI 自己生成的策略做元评估的展示——本质是"AI 给自己的产出打分"，
> 可信度存疑，且需要新增一整个评估节点。策略好不好，交给后面独立视角的 Hiring Manager 评分
> （见 4.6）来把关。

### 4.5 Run 详情页 — 编译/校验阶段

覆盖 Resume Editor → Patch Engine → Fact Validator → Hiring Manager 这个循环（最多跑 2 轮
返工）。

**展示内容**：
- 当前处于第几轮（比如"第 1 / 2 轮"）
- 当前节点（Resume Editor 正在改写 / Patch Engine 应用修改 / Fact Validator 校验中 / Hiring
  Manager 评审中）
- 如果某一轮事实校验没通过，需要清楚地标注"本轮未通过，正在返工"，并展示未通过的具体原因
  （见 4.6 事实校验问题列表的展示方式）

### 4.6 Human Gate ② — 最终审批页

进入条件：状态为"等待最终确认"。这是信息最密集的一页。

**事实校验结果**：通过/未通过的状态；如果有问题，逐条列出：严重程度（Critical/High/…）、
问题类型、问题描述、涉及的字段路径（可读形式）。

**Hiring Manager 评分**：
- 总分（满分 100）+ 决定（PASS / REVISE）
- 7 个分项，各自有自己的满分刻度：JD 核心匹配（30）、相关经历（20）、技术深度（15）、
  业务价值（10）、ATS 关键词（10）、清晰度（10）、可信度（5）
- 优势点列表、关注点列表、改进建议列表

**最终 Diff**：每一处改动一张卡片，展示：
- 目标字段（可读名称）+ 操作类型
- 修改前 / 修改后并排对比（改后被隐藏的字段要清楚标注"已隐藏"而不是留空）
- 修改理由
- 支撑事实（同 4.4，用于核实这条改动没有编造内容）

**操作**：
- "批准并导出最终 YAML"：确认使用当前候选版本，流程结束，产出可用文件
- "恢复完整原始版本"：放弃所有改动，候选版本恢复成 Master Resume 原样，重新校验
- "拒绝本次版本"：整个 Run 标记为已拒绝，到此结束
- **高级：人工微调**（默认折叠，进阶操作）：结构化的修改构建器——
  1. 从简历结构中选择目标字段（树状/层级选择，而不是要求用户记住内部路径写法）
  2. 选择操作类型（替换文本 / 调整顺序 / 隐藏 / 恢复）
  3. 根据操作类型出现对应的输入项（比如"替换文本"要填新内容；"调整顺序"要选新的排列）
  4. 提交后，系统会用同一套事实校验器重新检查，结果实时更新展示在本页

### 4.7 错误 / 中断状态

- **失败**：展示错误类型和消息；如果是事实校验失败，逐条列出具体问题（同 4.6 的问题列表
  展示方式）。根据失败的具体情况，提供对应的重试入口：
  - 有候选简历版本存在时：从候选版本重新校验
  - 其他情况：从原始 JD 重新跑一遍分析
- **已中断**（服务重启导致流程中断）：提供"从原 JD 重试分析"入口

### 4.8 在线查看

**入口**：独立的顶层导航；也可以从某个 Run 详情页直接跳转过去查看该 Run 的产出。

**选择版本**：一个下拉/列表选择器，选项包括：
- Master Resume（作为基准版本，永远存在）
- 各个 Run 产出的最终版本

**内容**：选中某个版本后，在页面主体区域展示排版后的简历效果（视觉上应与最终导出的 PDF
完全一致），支持中文/英文切换。

**不做的事**：不生成可对外分享的链接，不考虑移动端浏览体验优化，不考虑加载速度/SEO 这类
面向公开网站的指标。

> **设计稿评审确认（2026-09-02）**：版本历史只包含"内容真正发生变化"的节点——Master Resume
> （基准）和 Run 的最终批准版本。JD Analyzer / Resume Matcher / HR Reviewer 这几个分析阶段
> 不产出独立的简历快照（它们只输出分析类文本，不改动简历内容本身），所以**不在版本历史里
> 单独列出**，避免用户点开发现好几个"版本"内容完全一样。

### 4.9 LangSmith 调用链路入口

在 Run 详情页（尤其是分析中/编译中/失败状态）提供一个"查看完整调用记录"的外链入口，点击后
在新标签页打开对应的 LangSmith trace 页面。这不是本工具自己实现的功能展示区，只是一个跳转
入口。

## 5. Phase 2（后续增强，本次不需要出视觉稿，先了解需求即可）

- **Run 列表增强**：按公司分组/搜索筛选；标记某个 Run 为"最终采用版本"（比如同一个 JD 跑了
  两次，标出哪个是真正发出去的那份）；归档不再需要的 Run（不删除，只是从默认视图隐藏）
- **版本对比**：在同一个 JD/公司下，如果跑了多次 Run，能把两次的最终产出并排对比看差异

## 6. 明确不在范围内

- 视觉风格设计（配色、字体、图标、组件库等）——另行设计
- 鉴权、远程访问、多用户支持——当前是本地单机个人工具，这些留到后续评估
- 跨公司/跨 JD 的版本对比（比如对比"给 Google 的版本"和"给 Meta 的版本"）
- 移动端专项适配——默认桌面浏览器优先

## 7. 状态机参考（帮助理解每个页面在什么状态下出现）

| 状态 | 含义 | 对应页面 |
|---|---|---|
| ANALYZING / MATCHING / HR_REVIEWING / STRATEGIZING | 分析阶段各节点运行中 | 4.3 |
| WAITING_STRATEGY_APPROVAL | 等待 Human Gate ① | 4.4 |
| EDITING / APPLYING_PATCH / VALIDATING / REVIEWING / REVISING / HIRING_REVIEWED / FINALIZING | 编译/校验阶段各节点运行中，可能循环最多 2 轮 | 4.5 |
| WAITING_FINAL_APPROVAL | 等待 Human Gate ② | 4.6 |
| COMPLETED | 已批准导出，流程结束 | 只读展示 |
| FAILED | 失败（事实校验多轮未通过 / 运行异常） | 4.7 |
| INTERRUPTED | 服务重启导致中断 | 4.7 |
| REJECTED | 用户主动拒绝最终版本 | 只读展示 |
